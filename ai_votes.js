/* Placeholder until scripts/ai_vote.py runs. It overwrites this file with the
   AI proxy's blind votes (window.AI_VOTES = {model, soul_hash, votes:{…}}).
   While null, the "IA" voter simply doesn't appear in the affinity bar. */
window.AI_VOTES = null;
