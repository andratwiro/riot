# --- RIOT non-negotiables: never commit these ---
# soul.md = Rob's private political profile (the AI proxy's only input).
soul.md
soul.*.md
# Rob's manual ballot: lives in the browser session; the exported snapshot
# (⧉ Copy votes → this file, re-imported to resume) stays off the repo —
# the proxy must never be able to see it.
data/rob_votes.json
# Internal slide decks (company-internal material; this repo is public).
deck/

# --- large / re-fetchable source binaries ---
# The .txt extractions ARE committed; PDFs are re-fetchable via fetch_sessions*.py.
data/actas/*.pdf
# Brussels CRI PDFs — re-fetchable via fetch_sessions_bxl.py; .txt audit trail is kept.
data/brussels/cri_pdf/

# --- junk ---
.DS_Store
__pycache__/
*.pyc
node_modules/
.venv/
*.log

# expedient/source PDFs (re-fetchable from the portal; transcriptions are kept)
data/expedients/*.pdf
